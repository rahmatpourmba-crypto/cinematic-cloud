steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_IMAGE', '.']
    id: build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '$_IMAGE']
    id: push
images:
  - '$_IMAGE'
options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY